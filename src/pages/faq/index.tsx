import * as React from 'react';
import { Layout } from '../../layout';

export const FAQ: React.FC = () => {
    return <Layout>
        <h2>Frequently asked questions (FAQ).</h2>
        <h3>We're still working on this.</h3>
    </Layout>;
};
