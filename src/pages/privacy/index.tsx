import * as React from 'react';
import { Layout } from '../../layout';

export const Privacy: React.FC = () => {
    return <Layout>
        <h2>Privacy</h2>
        <p>We don't story any data about you or the codes you scan.</p>
        <p>No data, personal or otherwise, is ever transmitted (except for the bare technical necesities needed for website to download and run in your browser).</p>
    </Layout>;
};
